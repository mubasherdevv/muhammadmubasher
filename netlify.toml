[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NITRO_PRESET = "netlify"
  NPM_FLAGS = "--legacy-peer-deps"
  NODE_VERSION = "20"
  SECRETS_SCAN_ENABLED = "false"

[dev]
  command = "npm run dev"

